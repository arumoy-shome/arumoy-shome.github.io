---
title: Reproducible Machine Learning Pipelines Without a Framework, a Deliberately Long Title
date: 2025-03-01
abstract: |
    An abstract containing [a markdown link](https://example.org/alpha) and
    some *emphasis*, so that the listing fragment and the plain-text
    description can be told apart.
---

Body text for alpha.

The title above is long enough that `## [title](/blogs/alpha/)` exceeds
pandoc's default 72-column wrap, which is what makes this post a regression
test for `--wrap=none`.
